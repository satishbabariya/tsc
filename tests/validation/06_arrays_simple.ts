// Test array literals only
function testArrays() {
    // Array literals
    let numbers = [1, 2, 3, 4, 5];
    let mixed = [10, "hello", true];
    let empty = [];
    let nested = [[1, 2], [3, 4]];
    
    // Simple indexing
    let first = numbers[0];
    
    return 42;  // Return a simple value
}