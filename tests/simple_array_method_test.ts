// Simple test for array method support
let testArray: Array<number> = [1, 2, 3];

// Test basic array methods
let length = testArray.length;
let pushResult = testArray.push(4);
let popResult = testArray.pop();

console.log("Array method test completed successfully");